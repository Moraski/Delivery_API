server:
  servlet:
    context-path: /delivery/api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/API_Delivery
    username: root
    password: "04863147155"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

springdoc:
  swagger-ui:
    path: /public/swagger